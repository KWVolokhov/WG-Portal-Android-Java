Не анализировать Git
Не анализировать Gradle
Не проводить тестирования
Не пытатся пересохранять файлы, в которых изменений нет
Не анализировать и не менять файл с задания Tesklist.txt и другие текстовые файлы с заданиями
Не анализировать и не пытатся пересохранять файлы в которых, в которых поменялись только отступы
Не анализировать и не пытатся пересохранять файлы в которых, в которых поменялись комментарии
Если Java использует if() {...} с одним оператором, то оператор писать в одну строку с if и без {}
Метод или функция или процедура всегда ограничена 150 строками и не более чем 3-мя строками комментариев типа //
Класс с константами для SQL не делить ConstantsSQLDb.java
Один *.java файл не должен превышать 600 строк, в противном случае нужно искать возможность фунционально поделить класс
Всегда в конце выполнения Промта повторно проверяй в каждом измененном .java файле совпадение кол-ва скобок { и }
код писать совместимый с Android8
После выполнения КАЖДОЙ задачи для ИИ: обновлять архитектурные .md файлы в корне проекта — 
    ArchitectureUI.md (если затронут UI: экраны, контролы, меню, навигация), 
    ArchitectureLocalBackEnd.md (если затронуты данные: таблицы, менеджеры БД, record-классы), 
    ArchitectureServerSynkhro.md (если затронута синхронизация/сервер). 
    Правила ведения этих файлов описаны внутри каждого файла в разделе «Правила ведения файла (для ИИ)»
	
	




